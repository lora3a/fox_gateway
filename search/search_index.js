var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"index.html","title":"Fox Gateway","text":"<p>Documentation for the Fox Gateway</p>"},{"location":"dev.html","title":"Development","text":"<ol> <li> <p>Install Dev Requirements</p> <pre><code>python3 -m pip install -r dev.requirements.txt\n</code></pre> </li> <li> <p>install the git hook scripts</p> <pre><code>pre-commit install\n</code></pre> </li> </ol>"},{"location":"install.html","title":"Install On RoadRunner","text":"<ol> <li> <p>Login inside the RoadRunner</p> <pre><code>ssh acme@&lt;roadrunner ipaddr&gt;\n</code></pre> <p>Info</p> <pre><code>user: acme\n\npassword: acmesystems\n</code></pre> </li> <li> <p>Extend Root Fs</p> <pre><code>sudo extend_rootfs.sh\n</code></pre> </li> <li> <p>Update System</p> <pre><code>sudo apt update &amp;&amp; sudo apt upgrade\n</code></pre> </li> <li> <p>Install Dev Package</p> <pre><code>sudo apt install wget build-essential libncursesw5-dev libssl-dev libsqlite3-dev tk-dev libgdbm-dev libc6-dev libbz2-dev libffi-dev zlib1g-dev libncurses5-dev libnss3-dev libreadline-dev\n</code></pre> </li> <li> <p>Install Python 3</p> <pre><code>sudo apt install python3 python3-venv python3-pip libpq-dev\npython3 -m pip install --upgrade pip\npython3 -m pip install wheel\n</code></pre> </li> <li> <p>Download fox_gateway project</p> scpgit clone On PC<pre><code>git clone https://github.com/lora3a/fox_gateway.git\nscp -r fox_gateway/ acme@&lt;roadrunner ipaddr&gt;:~/acme/\n</code></pre> On RoadRunner<pre><code>sudo apt install git\ngit clone https://github.com/lora3a/fox_gateway.git\n</code></pre> </li> <li> <p>Add user to <code>dialout</code> group</p> <pre><code>sudo usermod -a -G dialout acme\n</code></pre> </li> <li> <p>install Gateway app</p> <pre><code>cd ~/fox_gateway\npython3 -m pip venv .venv\n. .venv/bin/activate\npython3 -m pip install -r requirements.txt\npython3 -m pip install .\n</code></pre> </li> </ol>"},{"location":"usage.html","title":"Fox Gateway Usage","text":"<p>Launch Gateway app</p> <pre><code>gateway --help\n</code></pre>"}]}